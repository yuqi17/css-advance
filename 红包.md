``` html

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <style>
        body {
            /* background: #f00; */
        }

        .mask {
            position: fixed;
            top: 0;
            left: 0;
            height: 100%;
            width: 100%;
            background-color: rgba(30, 30, 30, .7);

            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 10;
        }

        .box {
            background-color: #f00;
            height: 350px;
            width: 250px;
            border-radius: 10px;

            display: flex;
            flex-direction: column;
            /* justify-content: center; */
            align-items: center;

            position: relative;
        }

        .wrapper-layer {
            background-color: rgb(194, 70, 62);
            position: absolute;
            height: inherit;
            width: inherit;
            border-radius: inherit;
        }

        .bottom-layer {
            position: absolute;
            height: inherit;
            width: inherit;
            border-radius: inherit;
            background-color: rgb(208, 92, 73);
            clip-path: ellipse(68% 42% at 50% -10%);

            transition: all .3s;
        }

        /*  */

        .open {
            position: absolute;
            width: 60px;
            height: 60px;
            border-radius: 100%;
            background-color: gold;
            top: 80px;

            text-align: center;
            line-height: 60px;
            color: #fff;
            font-size: 28px;

            outline: none;

            z-index: 10;
        }

        .open:focus {
            opacity: 0;
        }

        .open:focus+.bottom-layer {
            clip-path: polygon(-68% 0%, 170% 0%, 50% 35%);
            background-color: rgb(180, 77, 60);
        }

        .open:focus+.bottom-layer>.coin {
            animation: bounce-up .3s forwards linear;
        }

        .coin {
            position: absolute;
            width: 85px;
            height: 85px;
            border-radius: 100%;
            background-color: gold;
            top: 147px;
            left: 82px;
        }


        @keyframes bounce-up {
            from {
                opacity: 0;
                top: 147px;
            }

            60% {
                opacity: 1;
                top: 100px;
            }

            75% {
                top: 80px;
            }

            90% {
                top: 55px;
            }
            95% {
                top: 62px;
            }

            to {
                top: 64px;
            }
        }

        .wrapper {
            margin-top: 191px;
            display: flex;
            flex-direction: column;
            justify-content: space-around;
            align-items: center;
            color: #fff;
        }

        .opened {
            opacity: 0;
            transform: translateY(20px)
        }

        .money {
            font-size: 40px;
        }

        .btn {
            width: 134px;
            border: 1px solid #fff;
            text-align: center;
            border-radius: 20px;
            padding: 6px;
        }

        .btn-close {
            height: 30px;
            width: 30px;
            border: 1px solid #fff;
            border-radius: 100%;

            top: -73px;
            right: -33px;
            position: absolute;
        }

        .btn-close>span:nth-child(1) {
            border-bottom: 1px solid #fff;
            width: 100%;
            display: inline-block;
            transform: rotateZ(-45deg);
        }

        .btn-close>span:nth-child(2) {
            border-bottom: 1px solid #fff;
            width: 100%;
            display: inline-block;
            transform: rotateZ(45deg);
            position: absolute;
            top: 16px;
        }
    </style>
</head>

<body>
    <div class="mask">
        <div class="box">
            <div class="btn-close" onclick="close_it()">
                <span></span>
                <span></span>
            </div>
            <div class="wrapper-layer">

                <div class="wrapper closed">
                    <div class="person">longmaoqiu</div>
                    <div class="info">给你发了一个红包</div>
                </div>

                <div class="wrapper opened">
                    <div class="message">恭喜你获得</div>
                    <div class="money">500</div>
                    <div class="btn">查看</div>
                </div>
            </div>

            <div class="open" onclick="open_it()" tabindex="0">開</div>

            <div class="bottom-layer">
                <div class="coin"></div>
            </div>
        </div>
    </div>
<!-- Code injected by live-server -->
<script type="text/javascript">
	// <![CDATA[  <-- For SVG support
	if ('WebSocket' in window) {
		(function () {
			function refreshCSS() {
				var sheets = [].slice.call(document.getElementsByTagName("link"));
				var head = document.getElementsByTagName("head")[0];
				for (var i = 0; i < sheets.length; ++i) {
					var elem = sheets[i];
					head.removeChild(elem);
					var rel = elem.rel;
					if (elem.href && typeof rel != "string" || rel.length == 0 || rel.toLowerCase() == "stylesheet") {
						var url = elem.href.replace(/(&|\?)_cacheOverride=\d+/, '');
						elem.href = url + (url.indexOf('?') >= 0 ? '&' : '?') + '_cacheOverride=' + (new Date().valueOf());
					}
					head.appendChild(elem);
				}
			}
			var protocol = window.location.protocol === 'http:' ? 'ws://' : 'wss://';
			var address = protocol + window.location.host + window.location.pathname + '/ws';
			var socket = new WebSocket(address);
			socket.onmessage = function (msg) {
				if (msg.data == 'reload') window.location.reload();
				else if (msg.data == 'refreshcss') refreshCSS();
			};
			if (sessionStorage && !sessionStorage.getItem('IsThisFirstTime_Log_From_LiveServer')) {
				console.log('Live reload enabled.');
				sessionStorage.setItem('IsThisFirstTime_Log_From_LiveServer', true);
			}
		})();
	}
	else {
		console.error('Upgrade your browser. This Browser is NOT supported WebSocket for Live-Reloading.');
	}
	// ]]>
</script></body>

<script>
    function $(selector) {
        return document.querySelector(selector);
    }

    function open_it() {
        $('.closed').style.display = 'none';

        $('.opened').style.opacity = 1;
        $('.opened').style.transition = 'all .3s'
        $('.opened').style.transform = 'translateY(0)'
    }

    function close_it() {
        $('.mask').style.display = 'none';
    }
</script>

</html>
```
